"""
Пути и общие настройки программы.

Пути вычисляются от каталога с исходным кодом, а не от текущего рабочего
каталога. Благодаря этому программа одинаково работает и при запуске из
своего каталога, и при запуске из любого другого места.
"""

import os

# Каталог, в котором лежит программа
BASE_DIR = os.path.dirname(os.path.abspath(__file__))

# Размеченный вручную набор таблиц: основа обучения и проверки качества
DATASET_PATH = os.path.join(BASE_DIR, "verified_dataset.json")

# Обученная модель классификации
MODEL_PATH = os.path.join(BASE_DIR, "trained_model.pkl")

# Набор таблиц, на котором работают тесты эвристики
TEST_TABLES_PATH = os.path.join(BASE_DIR, "table_for_test.html")

# Признаки, которые получает модель: доли типов данных в ячейках таблицы
FEATURE_NAMES = ("numeric", "form", "string", "media", "other", "no_data", "link")

# Служебный признак: прошла ли таблица проверку эвристикой.
# Равен 1, если структура корректна, и -1 в противном случае
CONSISTENCY_FEATURE = "consistency"

# Метка, которую получает таблица, не прошедшая проверку структуры
NO_GENUINE_LABEL = "no genuine"
